require_relative '../lib/Marie_christmas'

describe "the scrapping_mairie method" do
  it "blablabla" do
    expect(scrapping_mairie)).to eq(true)
    expect(scrapping_mairie)).to eq(true)
    expect(scrapping_mairie)).to eq(true)
  end
  it "blablabla" do
    expect(scrapping_mairie)).to eq(true)
    expect(scrapping_mairie)).to eq(true)
    expect(scrapping_mairie)).to eq(true)
  
  end
end
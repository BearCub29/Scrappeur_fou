require_relative '../lib/Dark_Trader'

describe "the scrapping_data method" do
  it "blablabla" do
    expect(scrapping_data(page)).to eq(true)
    expect(scrapping_data(page)).to eq(true)
    expect(scrapping_data(page)).to eq(true)
  end
  it "blablabla" do
    expect(scrapping_data(page)).to eq(true)
    expect(scrapping_data(page)).to eq(true)
    expect(scrapping_data(page)).to eq(true)
  
  end
end